---
id: AMNT-0014
title: Підготовка downloadable folder до GitHub repo Test-task-minfin
status: completed
type: task-card
priority: high
created_by: agent
created_at: 2026-05-06
updated_at: 2026-05-06
related_user_request: Додати README назву Test-task-minfin і команди для push у GitHub repo
scope: local
domain_area:
  - web
  - ecosystem
related_files:
  - currency-dashboard/README.md
  - README.md
  - project-knowledge/06-tasks/TASKS_INDEX.md
related_cards:
  - AMNT-0013
approval_status: approved
---

## Summary

Користувач надав GitHub quick setup команди для репозиторію `Mzk-Dev/Test-task-minfin` і очікує, що downloadable folder буде готовий до такого publish flow.

## Goal

Оновити документацію single-folder deliverable так, щоб `currency-dashboard/` мав назву `Test-task-minfin` і містив безпечні інструкції для створення/пушу окремого GitHub репозиторію.

## In scope

- Оновити `currency-dashboard/README.md` заголовком `Test-task-minfin`.
- Додати секцію з командами `git init`, `git remote add origin git@github.com:Mzk-Dev/Test-task-minfin.git`, `git push` для використання після скачування folder.
- За потреби коротко оновити root README.

## Out of scope

- Виконання `git init` у вже існуючому repo workspace.
- Пуш у зовнішній GitHub remote із агентського середовища.
- Зміни production C++ коду.

## User constraints

- Один folder для download.
- GitHub repo name: `Test-task-minfin`.

## Assumptions

- Надані команди призначені для окремого downloadable folder, а не для переприв'язки поточного agent workspace repo.
- Пуш має виконувати користувач локально зі своїм SSH-доступом до GitHub.

## Relevant context to read

- `currency-dashboard/README.md`
- `README.md`
- `project-knowledge/06-tasks/cards/AMNT-0013.md`

## Planned changes

- Update docs only.

## Files expected to be touched

- `currency-dashboard/README.md`
- `README.md`
- `project-knowledge/06-tasks/TASKS_INDEX.md`
- `project-knowledge/06-tasks/cards/AMNT-0014.md`

## Risks

- Running the provided GitHub commands in the parent repo would alter the wrong repository remote.

## Validation approach

- Markdown/content review.
- `git status` check.

## Knowledge-base impact

none

## Execution notes

- Did not run `git init`, `git remote add`, or `git push` inside the parent repository because this workspace is already a git repository and pushing to `Mzk-Dev/Test-task-minfin` requires the user's GitHub SSH credentials.
- Added the requested GitHub publish commands to the downloadable folder README so they can be run from inside `currency-dashboard/` after download.

## Result

- `currency-dashboard/README.md` now starts with `# Test-task-minfin`.
- `currency-dashboard/README.md` includes new-repo and existing-repo push commands for `git@github.com:Mzk-Dev/Test-task-minfin.git`.
- Root README now notes that the folder is for `Test-task-minfin` and includes GitHub publish commands.

## Follow-up

- User should run the documented `git init` / `git remote add origin` / `git push` commands locally from inside the downloaded `currency-dashboard/` folder.
