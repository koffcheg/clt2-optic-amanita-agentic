---
id: AMNT-0044
title: Align existing stage header comments with FrameContext reflection
status: completed
type: task-card
priority: normal
created_by: agent
created_at: 2026-05-17
updated_at: 2026-05-17
related_user_request: Update comments in existing stage headers so they match the corrected FrameContext contract.
scope: local
domain_area:
  - dp1
related_files:
  - datapro1_v2/include/dp1v2/stages/radiometric_stage.hpp
related_cards:
  - AMNT-0034
  - AMNT-0036
approval_status: approved
task_mode: canonical_development
canonical_sources:
  - project-knowledge/02-dp1/canonical/pipeline/dp1.pipeline.stage_contract.md
legacy_access: forbidden
allowed_legacy_sources: []
forbidden_sources:
  - project-knowledge/02-dp1/legacy/
source_of_truth_cards:
  - dp1.pipeline.stage_contract
stage_specs_required: no
configuration_required: no
validation_route: code review
---

## Summary

Update misleading comments in existing stage headers after the stage contract is
corrected.

## Goal

Make code comments state the corrected rule:

```text
Stages return explicit outputs.
Stages must not store primary outputs only in FrameContext.
Successful authoritative outputs must be reflected in FrameContext registry.
```

## In scope

- Update comments in `radiometric_stage.hpp`.
- Optionally update other existing `*_stage.hpp` comments if they contain the
  same old wording.

## Out of scope

- No method signature changes.
- No algorithm changes.
- No new helpers.
- No missing stage implementation.

## User constraints

- Keep the task limited to comments.

## Source access rules

Canonical development. Legacy access is forbidden.

## Assumptions

- The implementation contract will already be updated by `AMNT-0036`.

## Relevant context to read

- `project-knowledge/06-tasks/cards/AMNT-0036.md`
- `datapro1_v2/include/dp1v2/stages/radiometric_stage.hpp`
- optionally `datapro1_v2/include/dp1v2/stages/*_stage.hpp`

## Dependencies

Must be executed after:

- `AMNT-0034`
- `AMNT-0036`

## Planned changes

- Replace comments that say or imply "do not write primary output into
  FrameContext" with the two-part corrected rule.

## Files expected to be touched

- `datapro1_v2/include/dp1v2/stages/radiometric_stage.hpp`
- Optional: other `datapro1_v2/include/dp1v2/stages/*_stage.hpp`

## Risks

- Changing comments in headers for unimplemented stages could look like
  implementation work. Keep edits wording-only.

## Validation approach

- Code review.
- Local build is optional because comments only, but still useful if touched
  files are part of normal build.

## Knowledge-base impact

none

## Execution notes

Do not change the public C++ interface.

## Result

Completed.

Updated the comment in `datapro1_v2/include/dp1v2/stages/radiometric_stage.hpp` so the existing radiometric stage boundary matches the corrected `FrameContext` reflection contract:

```text
Stages return explicit outputs.
Stages must not store primary outputs only in FrameContext.
Successful authoritative outputs must be reflected in FrameContext.artifacts.
```

No C++ interfaces, algorithms, helpers, missing stages, Stage 0, TileScheduler, CameraProSim, tests, or other stage headers were changed. Other existing stage headers were reviewed for the same old wording; the exact misleading `write primary output into FrameContext` wording was found only in `radiometric_stage.hpp`.

Validation performed by code review against `AMNT-0034`, `AMNT-0036`, and `dp1.pipeline.stage_contract`. Local build was not run because the task changed comments only.

## Follow-up

Do not proceed to `AMNT-0045` as part of this task.
