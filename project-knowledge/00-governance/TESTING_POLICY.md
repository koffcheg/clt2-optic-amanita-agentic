# TESTING_POLICY.md

## Призначення

Цей файл описує правила тестування в проєкті та обмеження для AI-агента щодо створення, зміни й запуску тестів.

## Статус документа

Статус: draft

Цей документ є шаблоном.
Частина тестових правил може бути неузгодженою або неповною.
Поки правило не визначене явно, агент повинен діяти консервативно.

## Базове правило для агента

Без явного узгодження агенту заборонено:
- створювати нові automated tests;
- створювати fixtures, mocks, stubs, snapshots, golden files;
- додавати або змінювати тестову інфраструктуру;
- масово переписувати існуючі тести;
- змінювати тестову стратегію проєкту.

Якщо агент вважає, що тестування потрібне, він повинен:
- описати ризик;
- пояснити, чому тест був би корисний;
- запропонувати test plan;
- дочекатися узгодження.

## Роль тестування в проєкті

Тут потрібно описати, яку роль відіграє тестування в цьому проєкті.

Що варто визначити:
- чи тестування є обов’язковим для кожної зміни;
- чи допускаються задачі без тестів;
- чи є основний акцент на manual, integration або system-level перевірках;
- наскільки проєкт покладається на runtime validation замість unit tests.

Поточний статус:
TODO

## Типи тестів у проєкті

Тут потрібно описати, які типи тестів узагалі використовуються або плануються.

Можливі категорії:
- unit tests
- integration tests
- system / end-to-end tests
- smoke tests
- regression tests
- manual verification
- dataset-based validation
- performance / benchmark checks

Поточний статус:
TODO

## Коли тестування потрібне

Тут потрібно зафіксувати, в яких випадках бажано або обов’язково пропонувати тестування.

Що варто визначити:
- зміна бізнес-логіки;
- зміна алгоритму;
- зміна формату даних;
- зміна serialization / deserialization;
- зміна build/runtime environment;
- багфікс;
- рефакторинг без функціональної зміни;
- зміна конфігурації.

Поточний статус:
TODO

## Коли агент не повинен автоматично додавати тести

Агент не повинен автоматично додавати тести, якщо:
- задача цього прямо не вимагає;
- тестова стратегія для модуля не визначена;
- для коректного тесту потрібна нова інфраструктура;
- потрібні великі fixtures, datasets або mocks;
- очікуваний формат перевірки ще не узгоджений.

## Test plan замість автоматичного тесту

Якщо агент не має права створити тест, він повинен запропонувати test plan.

Test plan може включати:
- що саме потрібно перевірити;
- який тип перевірки підходить;
- які вхідні дані потрібні;
- який очікуваний результат;
- які ризики покриває перевірка;
- що можна перевірити вручну;
- що варто автоматизувати пізніше.

## Правила роботи з існуючими тестами

Тут потрібно визначити:
- чи можна агенту редагувати існуючі тести;
- у яких межах можна змінювати assertions;
- чи можна оновлювати snapshots;
- чи можна прибирати flaky tests;
- чи можна змінювати тестові дані.

Базове тимчасове правило:
Без явного узгодження агент не повинен масово змінювати наявні тести.
Локальні зміни можливі лише тоді, коли це прямо входить у погоджену задачу.

## Fixtures, mocks, stubs, datasets

Тут потрібно визначити:
- чи дозволені mocks;
- чи дозволені handwritten stubs;
- чи можна створювати тестові datasets;
- де зберігати fixtures;
- чи можна використовувати production-like data;
- як поводитися з великими бінарними test artifacts.

Поточний статус:
TODO

## Manual verification

Тут потрібно описати, які ручні перевірки прийнятні в проєкті.

Що варто визначити:
- коли manual verification достатня;
- як її описувати;
- який формат звіту очікується;
- чи потрібні приклади команд, логів, вхідних файлів, скріншотів або output artifacts.

Поточний статус:
TODO

## Performance та benchmark перевірки

Тут потрібно визначити:
- чи потрібні performance checks;
- чи можна агенту запускати benchmarks;
- чи можна додавати benchmark artifacts;
- як порівнювати результати;
- що вважається регресією.

Поточний статус:
TODO

## Запуск тестів

Тут потрібно визначити:
- які команди запуску тестів є допустимими;
- чи можна агенту запускати всі тести;
- чи можна запускати лише локальні або таргетовані перевірки;
- чи є обмеження по часу, ресурсах або середовищу;
- чи можна запускати тести в контейнері.

Поточний статус:
TODO

## Валідація без тестів

У проєкті може бути допустимим варіант, коли замість автоматичного тесту використовується інший спосіб підтвердження змін.

Тут потрібно визначити, які альтернативи прийнятні:
- локальний build;
- ручний сценарій перевірки;
- запуск конкретного pipeline;
- перевірка output files;
- перевірка логів;
- диф expected/actual artifact;
- перевірка на контрольному dataset.

Поточний статус:
TODO

## Обмеження на тестову інфраструктуру

Без явного узгодження агенту заборонено:
- додавати нові test frameworks;
- підключати нові mocking libraries;
- змінювати структуру test directories;
- додавати CI jobs лише для тестів;
- створювати великі тестові артефакти в репозиторії.

## Open questions

Тут потрібно збирати неузгоджені питання щодо тестування.

Приклад:
- Чи потрібні unit tests для core C++ logic, чи достатньо integration/manual validation?
- Чи допускається створення mocks у цьому проєкті?
- Який мінімальний test plan очікується для багфіксу?
- Чи можна агенту змінювати existing tests, якщо вони ламаються після погодженого refactoring?

## Definition of done для цього документа

Документ можна вважати робочим, коли:
- визначено, які типи тестів реально використовуються;
- зафіксовано, що агенту дозволено і що заборонено без узгодження;
- визначено, коли потрібен test plan;
- визначено, які ручні або альтернативні перевірки прийнятні;
- визначено правила для запуску тестів і зміни існуючої тестової бази.