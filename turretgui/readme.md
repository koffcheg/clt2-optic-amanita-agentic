GUI to control the turret and all cameras


# Dependancies install

>sudo apt install qt6-base-dev
sudo apt install libglx-dev libgl1-mesa-dev
>sudo apt install libxkbcommon-dev libjpeg-dev libwebp-dev libpng-dev libtiff-dev libdc1394-dev libavcodec-dev libavformat-dev libswscale-dev libgtk2.0-dev

